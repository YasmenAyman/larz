{
  "name": "larz",
  "private": true,
  "workspaces": [
    "laravel-foundation"
  ],
    "scripts": {
        "build": "npm --workspace laravel-foundation run build",
    "typecheck": "npm --workspace laravel-foundation exec -- tsc --noEmit",
    "lint": "npm --workspace laravel-foundation run lint"
  }
}
