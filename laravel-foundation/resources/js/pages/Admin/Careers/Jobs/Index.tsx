import { Head, Link, router } from '@inertiajs/react';
import AdminLayout from '@/layouts/AdminLayout';
import { Breadcrumbs, SearchInput, StatusBadge } from '@/components/admin/AdminLayoutParts';
export default function Index({ jobs, filters }: { jobs: { data: Array<{ id: number; title: string; department: string; location: string; published: boolean; featured: boolean }> }; filters: { search?: string } }) { return <AdminLayout><Head title="Jobs" /><div className="mx-auto max-w-6xl space-y-6 px-4 py-8 sm:px-6 lg:px-8"><div className="flex items-end justify-between"><div><Breadcrumbs items={['Admin', 'Jobs']} /><h1 className="mt-3 text-3xl font-semibold">Job vacancies</h1></div><Link href="/admin/jobs/create" className="rounded-lg bg-emerald-600 px-4 py-2 text-sm">Create job</Link></div><SearchInput value={filters.search ?? ''} onChange={(value) => router.get('/admin/jobs', { search: value }, { preserveState: true })} /><div className="overflow-x-auto rounded-xl border border-slate-800 bg-slate-950"><table className="w-full text-left text-sm"><thead className="border-b border-slate-800 text-xs uppercase text-slate-500"><tr><th className="px-4 py-3">Title</th><th className="px-4 py-3">Department</th><th className="px-4 py-3">Location</th><th className="px-4 py-3">Status</th><th className="px-4 py-3" /></tr></thead><tbody>{jobs.data.map((job) => <tr key={job.id} className="border-b border-slate-800/70"><td className="px-4 py-4">{job.title}</td><td className="px-4 py-4 text-slate-400">{job.department}</td><td className="px-4 py-4 text-slate-400">{job.location}</td><td className="px-4 py-4"><StatusBadge status={job.published ? 'Published' : 'Draft'} /></td><td className="px-4 py-4"><Link href={`/admin/jobs/${job.id}/edit`} className="text-emerald-400">Edit</Link></td></tr>)}</tbody></table></div></div></AdminLayout>; }
