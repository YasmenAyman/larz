import { Head, useForm, usePage } from '@inertiajs/react';
import { useState } from 'react';
import type { PageProps } from '@/types';
import AdminLayout from '@/layouts/AdminLayout';
import { Breadcrumbs, FormField, ImageUploadField, Notification } from '@/components/admin/AdminLayoutParts';

type Post = { id?: number; type: string; media_category_id?: number | null; title: string; slug: string; excerpt: string; content: string; event_date: string; published_at: string; is_featured: boolean; is_published: boolean; sort_order: number; seo_title: string; seo_description: string; featured_image?: string | null; open_graph_image?: string | null; translations?: { en?: Record<string, string>; ar?: Record<string, string> } };

export default function Form({ post, categories }: { post: Post | null; categories: Array<{ id: number; name: string }> }) {
    const { flash } = usePage<PageProps<{ flash?: { success?: string } }>>().props;
    const [language, setLanguage] = useState<'en' | 'ar'>('en');
    const { data, setData, post: create, put, processing, errors } = useForm<Post>({ type: post?.type ?? 'blog', media_category_id: post?.media_category_id ?? null, title: post?.title ?? '', slug: post?.slug ?? '', excerpt: post?.excerpt ?? '', content: post?.content ?? '', event_date: post?.event_date ?? '', published_at: post?.published_at ?? '', is_featured: post?.is_featured ?? false, is_published: post?.is_published ?? false, sort_order: post?.sort_order ?? 0, seo_title: post?.seo_title ?? '', seo_description: post?.seo_description ?? '', translations: { en: post?.translations?.en ?? {}, ar: post?.translations?.ar ?? {} } });
    const arabic = data.translations?.ar ?? {};
    const setArabic = (key: string, value: string) => setData('translations', { ...data.translations, ar: { ...arabic, [key]: value } });
    const submit = (event: React.FormEvent) => { event.preventDefault(); post?.id ? put(`/admin/media/posts/${post.id}`, { forceFormData: true }) : create('/admin/media/posts', { forceFormData: true }); };

    return <AdminLayout><Head title={post ? 'Edit Media Post' : 'Create Media Post'} /><div className="mx-auto max-w-4xl space-y-6 px-4 py-8 sm:px-6 lg:px-8"><Breadcrumbs items={['Admin', 'Media', post ? 'Edit' : 'Create']} /><h1 className="text-3xl font-semibold">{post ? 'Edit media post' : 'Create media post'}</h1><Notification message={flash?.success} /><form onSubmit={submit} encType="multipart/form-data" className="space-y-6 rounded-xl border border-slate-800 bg-slate-950 p-6"><div className="flex gap-2 border-b border-slate-800 pb-3"><button type="button" onClick={() => setLanguage('en')} className={`rounded-lg px-4 py-2 text-sm ${language === 'en' ? 'bg-gold text-black' : 'bg-white/5 text-white/60'}`}>English</button><button type="button" onClick={() => setLanguage('ar')} className={`rounded-lg px-4 py-2 text-sm ${language === 'ar' ? 'bg-gold text-black' : 'bg-white/5 text-white/60'}`}>Arabic</button></div><div className="grid gap-5 sm:grid-cols-2"><FormField label="Type"><select value={data.type} onChange={(event) => setData('type', event.target.value)} className="w-full rounded-lg border border-slate-700 bg-slate-900 px-3 py-2"><option value="press">Press release</option><option value="blog">Blog post</option></select></FormField><FormField label="Category"><select value={data.media_category_id ?? ''} onChange={(event) => setData('media_category_id', event.target.value ? Number(event.target.value) : null)} className="w-full rounded-lg border border-slate-700 bg-slate-900 px-3 py-2"><option value="">No category</option>{categories.map((category) => <option key={category.id} value={category.id}>{category.name}</option>)}</select></FormField></div>{language === 'en' ? <><FormField label="Title"><input value={data.title} onChange={(event) => setData('title', event.target.value)} className="w-full rounded-lg border border-slate-700 bg-slate-900 px-3 py-2" /></FormField><FormField label="Excerpt"><textarea rows={3} value={data.excerpt} onChange={(event) => setData('excerpt', event.target.value)} className="w-full rounded-lg border border-slate-700 bg-slate-900 px-3 py-2" /></FormField><FormField label="Rich content"><textarea rows={10} value={data.content} onChange={(event) => setData('content', event.target.value)} className="w-full rounded-lg border border-slate-700 bg-slate-900 px-3 py-2" /></FormField></> : <div dir="rtl" className="space-y-5"><FormField label="Title"><input value={arabic.title ?? ''} onChange={(event) => setArabic('title', event.target.value)} className="w-full rounded-lg border border-slate-700 bg-slate-900 px-3 py-2" /></FormField><FormField label="Excerpt"><textarea rows={3} value={arabic.excerpt ?? ''} onChange={(event) => setArabic('excerpt', event.target.value)} className="w-full rounded-lg border border-slate-700 bg-slate-900 px-3 py-2" /></FormField><FormField label="Rich content"><textarea rows={10} value={arabic.content ?? ''} onChange={(event) => setArabic('content', event.target.value)} className="w-full rounded-lg border border-slate-700 bg-slate-900 px-3 py-2" /></FormField></div>}<FormField label="Slug"><input value={data.slug} onChange={(event) => setData('slug', event.target.value)} className="w-full rounded-lg border border-slate-700 bg-slate-900 px-3 py-2" /></FormField><div className="grid gap-5 sm:grid-cols-2"><ImageUploadField label="Featured image" onChange={(file) => setData('featured_image', file as never)} /><ImageUploadField label="Open graph image" onChange={(file) => setData('open_graph_image', file as never)} /></div><button disabled={processing} className="rounded-lg bg-emerald-600 px-5 py-3 text-sm font-medium text-white disabled:opacity-50">{processing ? 'Saving...' : 'Save post'}</button></form></div></AdminLayout>;
}
