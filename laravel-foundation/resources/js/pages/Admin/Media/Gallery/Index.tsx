import { Head, useForm } from '@inertiajs/react';
import AdminLayout from '@/layouts/AdminLayout';
import { Breadcrumbs, FormField, ImageUploadField } from '@/components/admin/AdminLayoutParts';

export default function Index({ items }: { items: { data: Array<{ id: number; title: string | null; image: string | null; location: string | null; is_active: boolean }>; current_page: number; last_page: number } }) {
    const { data, setData, post, processing } = useForm({ image: null as File | null, title: '', alt_text: '', caption: '', location: '', event_date: '', sort_order: 0, is_published: true, is_active: true });
    return <AdminLayout><Head title="Photo Gallery" /><div className="mx-auto max-w-6xl space-y-6 px-4 py-8 sm:px-6 lg:px-8"><Breadcrumbs items={['Admin', 'Photo Galleries']} /><h1 className="text-3xl font-semibold">Photo gallery</h1><form onSubmit={(event) => { event.preventDefault(); post('/admin/photo-galleries', { forceFormData: true }); }} className="grid gap-4 rounded-xl border border-slate-800 bg-slate-950 p-5 sm:grid-cols-2"><FormField label="Title"><input value={data.title} onChange={(event) => setData('title', event.target.value)} className="w-full rounded-lg border border-slate-700 bg-slate-900 px-3 py-2" /></FormField><ImageUploadField label="Image" onChange={(file) => setData('image', file)} /><FormField label="Alt text"><input value={data.alt_text} onChange={(event) => setData('alt_text', event.target.value)} className="w-full rounded-lg border border-slate-700 bg-slate-900 px-3 py-2" /></FormField><FormField label="Location"><input value={data.location} onChange={(event) => setData('location', event.target.value)} className="w-full rounded-lg border border-slate-700 bg-slate-900 px-3 py-2" /></FormField><button disabled={processing} className="rounded-lg bg-emerald-600 px-4 py-2 text-sm">Upload photo</button></form><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{items.data.map((item) => <article key={item.id} className="overflow-hidden rounded-xl border border-slate-800 bg-slate-950"><img src={item.image ?? ''} alt={item.title ?? ''} className="h-40 w-full object-cover" /><div className="p-4"><h2 className="font-medium">{item.title}</h2><p className="mt-1 text-xs text-slate-500">{item.location}</p></div></article>)}</div></div></AdminLayout>;
}
