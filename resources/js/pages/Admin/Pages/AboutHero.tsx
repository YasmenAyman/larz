import { Head, useForm, usePage } from '@inertiajs/react';
import { useState } from 'react';
import type { PageProps } from '@/types';
import AdminLayout from '@/layouts/AdminLayout';
import { Breadcrumbs, FormField, Notification } from '@/components/admin/AdminLayoutParts';
import { useI18n } from '@/i18n';

type Copy = { eyebrow?: string; heading?: string; description?: string; cta_label?: string; cta_url?: string; secondary_cta_label?: string; secondary_cta_url?: string };

export default function AboutHero({ translations, background }: { translations: { en?: Copy; ar?: Copy }; background: string | null }) {
    const { flash } = usePage<PageProps<{ flash?: { success?: string } }>>().props;
    const { locale } = useI18n();
    const [language, setLanguage] = useState<'en' | 'ar'>(locale === 'ar' ? 'ar' : 'en');
    const form = useForm<{ translations: { en: Copy; ar: Copy }; background_image: File | null }>({ translations: { en: translations.en ?? {}, ar: translations.ar ?? {} }, background_image: null });
    const copy = form.data.translations[language];
    const update = (key: keyof Copy, value: string) => form.setData('translations', { ...form.data.translations, [language]: { ...copy, [key]: value } });
    const submit = (event: React.FormEvent) => { event.preventDefault(); form.transform((payload) => ({ ...payload, _method: 'put' })); form.post('/admin/pages/about/hero', { forceFormData: true, preserveScroll: true }); };

    return <AdminLayout><Head title="About Hero" /><div className="mx-auto max-w-6xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><div><Breadcrumbs items={['Dashboard', 'Website Pages', 'About Us', 'Hero']} /><h1 className="mt-3 text-3xl font-semibold">About hero</h1><p className="mt-2 text-sm text-slate-400">Manage the background image and both hero CTAs in English and Arabic.</p></div><Notification message={flash?.success} /><form onSubmit={submit} encType="multipart/form-data" className="space-y-6"><div className="flex gap-2 border-b border-slate-800 pb-3"><button type="button" onClick={() => setLanguage('en')} className={`rounded-lg px-4 py-2 text-sm ${language === 'en' ? 'bg-[#C5A880] text-black' : 'bg-white/5 text-white/60'}`}>English</button><button type="button" onClick={() => setLanguage('ar')} className={`rounded-lg px-4 py-2 text-sm ${language === 'ar' ? 'bg-[#C5A880] text-black' : 'bg-white/5 text-white/60'}`}>Arabic</button></div><section dir={language === 'ar' ? 'rtl' : 'ltr'} className="grid gap-5 rounded-xl border border-slate-800 bg-slate-950 p-6 md:grid-cols-2"><FormField label="Eyebrow"><input value={copy.eyebrow ?? ''} onChange={(event) => update('eyebrow', event.target.value)} className="w-full rounded-lg border border-slate-700 bg-slate-900 px-3 py-2" /></FormField><FormField label="Heading"><textarea rows={4} value={copy.heading ?? ''} onChange={(event) => update('heading', event.target.value)} className="w-full rounded-lg border border-slate-700 bg-slate-900 px-3 py-2" /></FormField><FormField label="Description"><textarea rows={4} value={copy.description ?? ''} onChange={(event) => update('description', event.target.value)} className="w-full rounded-lg border border-slate-700 bg-slate-900 px-3 py-2" /></FormField><FormField label="Primary CTA label"><input value={copy.cta_label ?? ''} onChange={(event) => update('cta_label', event.target.value)} className="w-full rounded-lg border border-slate-700 bg-slate-900 px-3 py-2" /></FormField><FormField label="Primary CTA URL"><input value={copy.cta_url ?? ''} onChange={(event) => update('cta_url', event.target.value)} className="w-full rounded-lg border border-slate-700 bg-slate-900 px-3 py-2" /></FormField><FormField label="Secondary CTA label"><input value={copy.secondary_cta_label ?? ''} onChange={(event) => update('secondary_cta_label', event.target.value)} className="w-full rounded-lg border border-slate-700 bg-slate-900 px-3 py-2" /></FormField><FormField label="Secondary CTA URL"><input value={copy.secondary_cta_url ?? ''} onChange={(event) => update('secondary_cta_url', event.target.value)} className="w-full rounded-lg border border-slate-700 bg-slate-900 px-3 py-2" /></FormField></section><section className="rounded-xl border border-slate-800 bg-slate-950 p-6"><h2 className="text-xl font-semibold">Background image</h2>{background && <img src={background} alt="Current about hero background" className="mt-4 h-48 w-full rounded-xl object-cover" />}<input type="file" accept="image/*" onChange={(event) => form.setData('background_image', event.target.files?.[0] ?? null)} className="mt-4 block w-full rounded-lg border border-slate-700 bg-slate-900 px-3 py-2 text-sm text-slate-300" /></section><button type="submit" disabled={form.processing} className="rounded-lg bg-emerald-600 px-5 py-3 text-sm font-medium text-white disabled:opacity-50">{form.processing ? 'Saving...' : 'Save About hero'}</button></form></div></AdminLayout>;
}
