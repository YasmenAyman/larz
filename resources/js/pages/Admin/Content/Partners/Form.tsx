import { Head, Link, useForm, usePage } from '@inertiajs/react';
import { useState } from 'react';
import type { PageProps } from '@/types';
import AdminLayout from '@/layouts/AdminLayout';
import { Breadcrumbs, FormField, Notification } from '@/components/admin/AdminLayoutParts';

type Copy = { name: string; role: string; description: string };
type Partner = { id?: number; name: string; role: string; description: string; url: string; sort_order: number; is_published: boolean; logo?: any; translations?: { en?: Partial<Copy>; ar?: Partial<Copy> } };
const inputClass = 'w-full rounded-xl border border-white/15 bg-[#1e1e22] px-4 py-3 text-sm text-white outline-none transition focus:border-[#C5A880] focus:ring-1 focus:ring-[#C5A880]/30';

export default function Form({ partner }: { partner: Partner | null }) {
    const { flash } = usePage<PageProps<{ flash?: { success?: string } }>>().props;
    const [language, setLanguage] = useState<'en' | 'ar'>('en');
    const form = useForm<Partner & { translations: { en: Copy; ar: Copy }; logo: File | null }>({ name: partner?.name ?? '', role: partner?.role ?? '', description: partner?.description ?? '', url: partner?.url ?? '', sort_order: partner?.sort_order ?? 0, is_published: partner?.is_published ?? true, logo: null, translations: { en: { name: partner?.translations?.en?.name ?? partner?.name ?? '', role: partner?.translations?.en?.role ?? partner?.role ?? '', description: partner?.translations?.en?.description ?? partner?.description ?? '' }, ar: { name: partner?.translations?.ar?.name ?? '', role: partner?.translations?.ar?.role ?? '', description: partner?.translations?.ar?.description ?? '' } } });
    const errors = form.errors;
    const copy = form.data.translations[language];
    const update = (key: keyof Copy, value: string) => form.setData('translations', { ...form.data.translations, [language]: { ...copy, [key]: value } });
    const submit = (event: React.FormEvent) => { event.preventDefault(); form.transform((payload) => ({ ...payload, name: payload.translations.en.name || payload.translations.ar.name, _method: partner?.id ? 'put' : undefined })); partner?.id ? form.post(`/admin/pages/about/partners/${partner.id}`, { forceFormData: true }) : form.post('/admin/pages/about/partners', { forceFormData: true }); };

    return <AdminLayout><Head title={partner ? 'Edit Partner' : 'Add Partner'} /><div className="mx-auto max-w-3xl space-y-8"><div><Breadcrumbs items={['Dashboard', 'Website Pages', 'About Us', 'Partners', partner ? 'Edit' : 'Add']} /><h1 className="mt-2 text-2xl font-bold text-white sm:text-3xl">{partner ? 'Edit partner' : 'Add partner'}</h1></div><Notification message={flash?.success} /><form onSubmit={submit} encType="multipart/form-data" className="space-y-5 rounded-2xl border border-white/10 bg-[#161619]/90 p-6 shadow-xl"><div className="flex gap-2 border-b border-white/10 pb-3"><button type="button" onClick={() => setLanguage('en')} className={`rounded-lg px-4 py-2 text-sm ${language === 'en' ? 'bg-[#C5A880] text-black' : 'bg-white/5 text-white/60'}`}>English</button><button type="button" onClick={() => setLanguage('ar')} className={`rounded-lg px-4 py-2 text-sm ${language === 'ar' ? 'bg-[#C5A880] text-black' : 'bg-white/5 text-white/60'}`}>Arabic</button></div><div dir={language === 'ar' ? 'rtl' : 'ltr'} className="space-y-5"><div className="grid gap-5 sm:grid-cols-2"><FormField label="Name" error={errors.name}><input required value={copy.name} onChange={(event) => update('name', event.target.value)} className={inputClass} placeholder="Partner name" /></FormField><FormField label="Role / affiliation"><input value={copy.role} onChange={(event) => update('role', event.target.value)} className={inputClass} placeholder="Architecture & design" /></FormField></div><FormField label="Description"><textarea rows={3} value={copy.description} onChange={(event) => update('description', event.target.value)} className={inputClass} /></FormField></div><FormField label="Website URL" error={errors.url}><input type="url" value={form.data.url} onChange={(event) => form.setData('url', event.target.value)} className={inputClass} placeholder="https://example.com" /></FormField><div className="grid gap-5 sm:grid-cols-2"><FormField label="Sort order"><input type="number" min="0" value={form.data.sort_order} onChange={(event) => form.setData('sort_order', Number(event.target.value))} className={inputClass} /></FormField><FormField label="Logo image" error={errors.logo}><input type="file" accept="image/*" onChange={(event) => form.setData('logo', event.target.files?.[0] ?? null)} className={inputClass} />{partner?.logo && <img src={partner.logo} alt="Current logo" className="mt-3 h-20 w-auto rounded-lg border border-white/10 bg-white/5 object-contain p-2" />}</FormField></div><label className="flex items-center gap-3 text-sm text-white/75"><input type="checkbox" checked={form.data.is_published} onChange={(event) => form.setData('is_published', event.target.checked)} /> Published</label><div className="flex justify-end gap-3 border-t border-white/10 pt-5"><Link href="/admin/pages/about/partners" className="rounded-xl border border-white/15 px-5 py-3 text-sm text-white/75">Cancel</Link><button disabled={form.processing} className="rounded-xl bg-gradient-to-r from-[#C5A880] to-[#D4AF37] px-5 py-3 text-sm font-semibold text-black disabled:opacity-50">{form.processing ? 'Saving...' : partner ? 'Save changes' : 'Create partner'}</button></div></form></div></AdminLayout>;
}
